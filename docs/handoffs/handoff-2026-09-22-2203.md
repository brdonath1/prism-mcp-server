# PRISM checkpoint compatibility preparation
Agent: codex · Model: GPT-6 Astra / Medium · Session label: optimization codex-004
Supersedes: archival .prism/handoff.md; implementation continuation from codex-claude-optimization/docs/handoffs/handoff-2026-09-22-workflow-prism.md
Branch: codex/004-prism-checkpoint-projection; main before publication: 72151b7cea501dc1cad0765a168ecfa958dc04e0
Merged: reader #144 at the above main. This preparation unit is pending publication.

## 0. Read this first
The operator authorized continuing PRISM writer consolidation after the Codex lifecycle transfer. Session 003 is now natively archived and journal-closed; session 004 owns the optimization work. This unit adds a read-only compatibility preparation action, not a new remote publisher. The operator explicitly approved updating only current server-version references in CLAUDE.md; both now match 4.15.3.

## 1. Operator direction and authorizations
Improve workflow speed/cost while preserving quality, portable knowledge, CI and provider ownership. Canonical Local Development clones only; no new worktrees, provider launches/settings changes, paid services or background automations. Canonical dated handoffs still publish through the repository PR/checks contract. No production finalize was invoked. The operator approved “Allow version-reference edits only”; only the current server version and architecture-banner version changed in CLAUDE.md. Historical version notes and provider instructions are preserved.

## 2. Source and implementation
`src/utils/published-checkpoint-projection.ts` verifies current main before and after revision-pinned reads. It checks the expected dated path/blob against LATEST, reads existing native template metadata at the same revision, preserves native root/.prism layout, and returns a deterministic schema-compatible pointer to the complete canonical checkpoint. No model composes a second narrative.
`prism_finalize action=prepare_checkpoint` exposes this candidate with explicit no-write/not-finalized fields. The source argument is rejected on other actions before any work, avoiding an implied commit guard. Existing audit/draft/commit/full remain unchanged. Version 4.15.3 metadata and transaction documentation accompany the unit. No new dependencies or environment changes.

## 3. Release and verification
Build and lint passed. Initial full local suite: 2,420 passed, six pre-existing skips, one version-reference failure; the subsequently authorized reference correction passed all 15 tests in the affected release suite. New projection and tool-action suites pass; compatibility tests cover normal draft commits. Tests use mocked dependencies; no live mutating acceptance was performed. Node 18/20 CI run 35812657157 passed at 73ba288. Contract run 35812657172 caught the new handoff filename format; the handoff and pointer were corrected to the required timestamp form. Final required-check verification remains the merge gate. Root reviewed integration; Terra/Medium implemented the bounded projection helper/tests. No independent security review was required for this read-only action.
`apex-deploy --dry-run` reported this older service has no .metaswarm/stream and performed no deployment. Existing main-connected Railway deployment remains the release route. No deployment is claimed.

## 4. Decisions
- Keep canonical handoff publication PR-only. Derive a compatibility pointer rather than independently authoring duplicate work-state content.
- Require a current pinned source and fail closed on missing metadata or read-time main drift. Preparation is not a reservation or guarantee of later freshness.
- Preserve historical handoff checks and legacy native file layout. Do not infer automatic consumer adoption or lifecycle completion from a prepared file.

## 5. State at snapshot
Canonical repo `/Users/brdonath/development/prism-mcp-server`, owned branch above, based on reader release main. No overlapping PR was open at scoping. Preserve old `codex/prism-published-checkpoint` references. Optimization repo remains on `codex/003-authorized-lifecycle-recovery`; private lifecycle receipts remain there. Credentials, provider configuration and parked automation state unchanged.

## 6. Continuation
Publish this unit by PR and require build-and-test (18), build-and-test (20) and contract green. Verify deployment availability once after the normal main-connected release. Then observe preparation at a normal authorized checkpoint and design guarded publication/client adoption separately. Broader queue: proportional knowledge maintenance, installer/source reconciliation preserving ownership, optional integrations only with measured need. Do not claim publication-count or cost savings from this preparation-only unit.

## 7. Landmines
A candidate can become stale after preparation; reverify canonical source and history before existing authorized publication. The action checks pointer/target agreement, not historical ordering of every dated handoff. Existing hook-only living-document restrictions remain. Old clients must follow the complete canonical reference before acting. Native template metadata that cannot be parsed is refused, not invented.

## 8. Required reading
Read AGENTS.md, docs/publication-transactions.md and the projection/tool tests before changes. The optimization handoff carries the larger objective and prior release evidence; native receipts, not published titles, establish lifecycle completion.

Read docs/handoffs/handoff-2026-09-22-2203.md and verify checkpoint preparation publication and continue client adoption.
