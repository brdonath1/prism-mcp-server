:{$PORT} {
    log {
        output stderr
    }
    reverse_proxy localhost:8082 {
        header_up Authorization "Bearer {$GITHUB_PERSONAL_ACCESS_TOKEN}"
        header_up X-MCP-Toolsets "{$GITHUB_TOOLSETS}"
    }
}
