node_modules/
dist/
.env
*.js.map
*.tsbuildinfo
scripts/*
!scripts/check-model-freshness.mjs

# Dispatch state lives in the brdonath1/prism-dispatch-state repo (D-123 /
# SRV-117), never in this repo — keep stray local records untracked.
.dispatch/
